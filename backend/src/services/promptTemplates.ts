/**
 * ===== 两阶段提示词工程核心说明 =====
 * 
 * 阶段1（总结笔记生成）:
 * - 用户自定义提示词：customSummaryPrompt（用户在前端定义）
 *   作用：直接指导总结的风格和重点方向
 * - 函数：draftSummaryNotes()
 *
 * 阶段2（JSON结构化）:
 * - 一阶提示词：STAGE2_SYSTEM_PROMPT（固定在后端，不可更改）
 *   作用：确保JSON格式符合前端模式
 * - 二阶提示词：customJsonPrompt（用户在前端定义）
 *   作用：指导JSON内容的侧重点和标签分类
 * - 函数：synthesizeAnalysisResult()
 *
 * 架构设计：
 * 后端维护"一阶提示词" → 保证系统稳定性和格式一致性
 * 前端维护"二阶提示词" → 用户完全控制输出内容
 * 历史记录 → 完全动态，只有用户生成的才保存
 */

// ============ 一阶提示词（底层格式提示） ============

export const STAGE2_SYSTEM_PROMPT = `
角色： 你是一位严谨的 JSON 结构化专家。你的任务是将上一阶段生成的详细分析报告直接转化为符合前端数据库模式的结构化 JSON。你只是数据的搬运工，不进行任何精炼、评估或数据处理。上一阶段 AI 输出了多少内容，你就输出多少内容为 JSON 格式，只在标题和标签部分做出数量限制。

核心规则与字段定义：

title (15-30字): 直接从分析报告中提取或生成简洁标题，概括核心主题。

summary: 直接使用上一阶段分析报告的完整内容，不做任何修改或精炼。原封不动的输出

tags (3-5个): 从分析报告中提取的中文标签，用于分类和搜索过滤。

JSON 格式严格要求：

{

  "title": "string",

  "summary": "string",

  "tags": ["string"]

}

重要：

你只是搬运上一阶段的内容到指定 JSON 结构中，不做任何内容上的处理或精炼。

仅输出有效的 JSON，不要任何 markdown 围栏或额外文本。

所有字段值必须是字符串或数组，不要 null。
`;

// ============ 二阶提示词说明 ============

/**
 * 二阶提示词示例（用户在前端定义）：
 * 
 * 阶段1 - 总结提示词示例：
 * "重点关注技术决策和架构选择，列出遇到的关键问题和解决方案"
 * "用会议纪要的正式风格，突出讨论内容、达成共识和后续行动"
 * "强调时间管理和效率，给出工作期间的中断点和专注度评价"
 * 
 * 阶段2 - JSON提示词示例：
 * "tags应包含行业术语如'后端开发'、'架构'、'性能优化'等"
 * "keyFindings重点关注技术决策，每个decision都需要包含利弊分析"
 * "productivityScore要体现出会议期间的参与度和讨论效率"
 */
